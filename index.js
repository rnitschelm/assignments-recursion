// The tests are turned off by default (with an "x").
// When you are ready for the next test, remove the x from the xit.
// Good luck! :D


// When given a list of numbers, returns the sum
// Example: sum([1, 2, 3]) === 6
// Constraint: should use recursion
const sum = numbers => 0;

// When give a list of numbers, triples the numbers
// Example: triple([1, 2, 3]) === [3, 6, 9]
// Constraint: should use recursion
const triple = numbers => 0;

// When given a list of arrays with numbers, or other arrays with numbers, returns the sum
// Example: flatSum([1, [2], [[3]]) === 6
// Constraint: should use recursion
const flatSum = numbers => 0;

// When given an object with nested objects and possible string values, turns all strings
// to uppercase.
// Example: upper({ a: { b: "hello world!" } }) == { a: { b: "HELLO WORLD!" } }
const upper = object => {};

module.exports = {
  sum,
  flatSum,
  triple,
  upper
}
